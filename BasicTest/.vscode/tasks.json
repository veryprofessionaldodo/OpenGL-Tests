{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compile OpenGL",
            "type": "shell",
            "command": "g++ -lX11 -Iinclude -I/include/glad/*.h -lpthread -lXrandr -lXi -ldl -lglut -lGL -lglfw -lGLU -lGLEW src/*.c src/*.cpp -o Application",
            "problemMatcher": [],
            "group": "build"
        },
        {
            "label": "Run OpenGL Application",
            "type": "shell",
            "command": "./Application",
            "problemMatcher": [],
            "group": "build"
        }
    ]
}